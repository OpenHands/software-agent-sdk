Retrieves the status and output of a task.
- Takes a task_id parameter identifying the task
- Returns the task's status and output
- Set block=true (default) to wait for task completion, or block=false for a non-blocking status check
- Works with both background and foreground tasks